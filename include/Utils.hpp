/**
 * @file Utils.hpp
 * @brief Fichier regroupant les inclusions standards et l'espace de nommage.
 * Ce fichier permet d'éviter la redondance des includes dans le projet.
 */

#ifndef UTILS_HPP
#define UTILS_HPP

// Inclusions des bibliothèques standards vues en cours
#include <iostream> // Entrées/Sorties (cout, cin)
#include <string>   // Gestion des chaînes de caractères
#include <vector>   // Gestion des tableaux dynamiques (pour l'équipe)
#include <cstdlib>  // Gestion de l'aléatoire (rand, srand)
#include <ctime>    // Gestion du temps (time) pour la graine aléatoire
#include <algorithm> // Pour max()
using namespace std; // Utilisation de l'espace de nom standard

#endif